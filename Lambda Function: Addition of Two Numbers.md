# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
add=lambda a,b: a+b
print("Sum of given is",add(int(input("Enter any number")),int(input('Enter any number'))))
```
## Output
<img width="1051" height="171" alt="600890132-c70b15cb-49ea-4ca5-aeee-a3b771e2357a" src="https://github.com/user-attachments/assets/9c07ab16-0ef7-45ed-9bb1-ff75b4acc088" />


## Result
Successfully wrote a Python program that defines a lambda function which takes two arguments a and b, and returns their sum.
